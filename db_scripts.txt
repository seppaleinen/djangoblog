CREATE TABLE `database_directory` (
	`id`	INTEGER NOT NULL,
	`git_directory`	TEXT,
	`git_shortname`	TEXT,
	PRIMARY KEY(id)
);

CREATE TABLE `database_branch` (
	`id`	INTEGER NOT NULL,
	`git_shortname`	TEXT NOT NULL,
	`git_branch`	TEXT NOT NULL,
	PRIMARY KEY(id)
);